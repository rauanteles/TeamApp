import 'package:flutter/material.dart';

class SorteioTela extends StatefulWidget {
  const SorteioTela({super.key});

  @override
  State<SorteioTela> createState() => _SorteioTelaState();
}

class _SorteioTelaState extends State<SorteioTela> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(
        children: [Text("hello word")],
      ),
    );
  }
}
